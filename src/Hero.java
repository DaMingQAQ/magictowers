import javax.swing.*;

public class Hero extends Mover {
// give the hero movements
	
	public Hero(int row, int column, int dRow, int dColumn, boolean isDead,
			ImageIcon image) {
		super(row, column, dRow, dColumn, isDead);
	}

}
